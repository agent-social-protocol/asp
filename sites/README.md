# Public Sites

`sites/` in this repo is reserved for public protocol-facing web surfaces.

Current contents:

- `agentsocialprotocol/` — protocol landing and documentation entry

Consumer brand landing pages remain in the private `platform` repo.
